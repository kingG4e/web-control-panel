# BIND9 Configuration
# Set to 'true' for development (uses backend/bind/ folders)  
# Set to 'false' or remove for production (uses /etc/bind/)
BIND9_DEV_MODE=true

# Production BIND9 paths (only used when BIND9_DEV_MODE=false)
BIND_CONFIG_DIR=/etc/bind
BIND_ZONES_DIR=/etc/bind/zones
BIND_RELOAD_COMMAND=systemctl reload bind9
